server {
    listen 80;
    server_name your-domain.com;  # replace with your domain or IP

    # Redirect HTTP to HTTPS (uncomment after adding SSL cert via certbot)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://127.0.0.1:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # For PDF downloads
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;
    }
}

# HTTPS block (uncomment after running: sudo certbot --nginx -d your-domain.com)
# server {
#     listen 443 ssl;
#     server_name your-domain.com;
#
#     ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.key;
#
#     location / {
#         proxy_pass http://127.0.0.1:8001;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 120s;
#     }
# }
